import React from "react";

export default function Formulogin() {
  return;
  <div>
    <input
      type="text"
      className="form-control"
      placeholder="E-mail"
      id="email"
      name="email"
    />

    <input
      type="password"
      className="form-control"
      placeholder="Password"
      id="password"
      name="password"
    />
  </div>;
}
